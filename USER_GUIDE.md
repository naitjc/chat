# Chat RP 用户使用教程

这是一个本地运行的角色聊天应用。你可以选择预设角色或创建自己的角色，与角色自由聊天，也可以围绕一个最终目标推进互动故事。本指南按真实使用顺序编写，不需要了解任何开发知识。

## 手机用户一分钟上手

1. 打开应用后，先关闭欢迎提示。
2. 点击顶部的“存档”按钮。如果还没有存档，点击“新建”。
3. 只是想聊天就选择“自由模式”；希望围绕目标推进剧情就选择“故事模式”。
4. 点击创建好的存档，应用会自动返回聊天界面。
5. 在底部文本框输入内容，点击“发送”。聊天记录和关系状态会自动保存。

手机顶部保留三个主要入口：

- **存档**：选择或新建聊天存档，管理故事章节。
- **角色设置**：查看当前存档中的角色资料、好感度和情绪；未打开存档时不可使用。
- **更多**：切换或创建角色、打开教程、搜索消息、更换头像和背景、切换主题、导出对话，以及配置 Android 模型接口。

输入区将表情、文本框与发送按钮放在同一行；模型参数在“更多”中设置。阅读旧消息时，新回复不会强制拉回底部，可点击“有新消息 · 回到底部”继续跟随。回复结束后若仍在处理，会显示“正在整理记忆…”。

## 1. 开始使用

打开 Web 页面后，先在顶部选择角色。页面初始只显示聊天存档列表；点击具体存档后，才会打开聊天面板和角色设定面板。

如果没有合适的角色，可以在角色选择区域创建自定义角色，填写姓名、年龄、性格、语调和身份等信息。

## 2. 创建聊天存档

点击“选择聊天模式”，然后选择一种模式。

### 自由模式

自由模式没有主线、章节或预设结局，适合日常聊天、长期陪伴和随时切换话题。

创建时可以设置：

- 存档名称
- 初始好感度（0～100）

初始好感度只影响这一个存档，不会修改角色本身的默认设定。

### 故事模式 Beta

故事模式仍在完善中，适合围绕一个明确目标进行互动故事。

创建时需要填写最终目标，例如“和角色一起找到失落的星图”。故事模式的新存档从好感度 `0` 开始，关系阶段为“素昧平生”。

故事模式不会替用户决定行动，也不会自动跳过章节。模型只能提出章节或目标建议，是否接受由用户决定。

## 3. 存档与面板

- 第一次点击存档：打开角色设定和聊天面板。
- 再次点击当前已打开的存档：收起角色设定和聊天面板。
- 再次点击：重新打开该存档。
- 删除存档：会删除该存档的聊天记录、章节、关系状态和快照，操作不可恢复。

不同存档之间的聊天记录和关系状态相互独立。

## 4. 关系状态

角色状态面板会显示好感度、情绪值和关系阶段。

好感度与关系阶段分别记录：好感度表示喜欢的程度，情绪表示当下感受，关系阶段表示已经建立的关系。

- 新建存档好感度低于 25 时为“初识”，达到 25 后可以自然进入“熟悉”。高分不会自动带来信任、亲密或伴侣承诺。
- 打开聊天顶部的记忆面板，点击“关系”→“调整关系”，选择阶段，填写已经发生的互动依据，再点击“确认关系变化”。关系既可以进展，也可以在有依据时调整回较早阶段。
- 确认后的阶段不会随分数自动升降。旧存档原有的阶段继续保留，不会被强制重置。
- 情感分析结合近期对话、角色性格和记忆理解本轮消息。角色会在既有关系边界内表现语气和情绪，不会直接说出内部数值。

新建存档时会随机生成 `-10～10` 的初始情绪值。这个随机值只在创建时生成，重新打开存档不会重新随机。

## 5. 故事状态与章节

打开故事模式存档后，可以查看：

- 最终目标和目标状态
- 当前章节与章节数量
- 当前章节回顾或上一章提要
- 已确认的关键事件

当模型认为当前章节接近自然收束点时，会提出进入下一章的建议。用户可以选择：

- 继续当前章节
- 接受建议并进入下一章
- 手动结束当前章节

过去的章节会变成只读。如果想从过去的状态重新发展，可以从该章节创建分支故事。

## 6. 确认关键事件

打开“故事与记忆”→“记忆”→“编辑记忆”，在共同经历中记录明确认可的重要事实，例如：

> 我和角色约定下周一起调查旧车站。

确认后，这条内容会进入故事记忆，在后续章节和模型上下文中继续生效。普通闲聊不会自动写入长期故事记忆，避免故事背景越来越混乱。

### 记忆与进展

自由模式从聊天顶部打开“记忆与进展”，故事模式入口为“故事与记忆”。面板默认展示已保存内容，点击底部编辑按钮后才进入表单：

- **记忆**：重要事实与偏好、共同经历，每行一条。可以添加、修改或删除；模型不会自动改写这些内容。
- **事项**：故事模式记录当前地点和在场人物，两种模式都可以记录约定。每轮完整回复后尝试自动整理，用户可修正内容，并将事项标为“未完成”“已完成”或“已取消”。事项最多保留 40 条，达到容量后需手动整理，不会自动丢弃旧事项。
- **关系**：点击“调整关系”，根据明确事实确认关系阶段；记忆和事项编辑后点击“保存修改”。取消编辑会放弃草稿，关闭尚未保存的面板时会提醒确认。
- **故事**（仅故事模式）：集中展示最终目标和章节回顾，可以修改目标或重新开启已达成的目标。章节切换仍在聊天存档中操作。

模型整理的内容可能误判；界面显示原文依据供核对，但原文匹配不代表语义判断一定正确。整理失败会显示提示，完整回复仍然保留，旧场景与事项保持不变。场景和事项跟随章节快照保存，从旧章节分支不会继承未来章节的变化。回复生成中和已归档章节不能修改记忆。

## 7. 上下文容量与继承对话

模型能读取的历史内容是有限的。上下文占用升高时，聊天顶部会显示估算百分比和容量提醒；平时收起数值，减少对阅读的干扰。

系统按中文与英文文本估算容量，并计入角色设定和记忆。近期对话达到预算时自动整理较早内容，优先保留完整的近期对话轮次；聊天界面的原始记录不受压缩影响。摘要失败时本次发送会报错，可重试，不会静默截断历史。

大约 `70%`、`85%` 和 `95%` 时会分级提醒，也可点击“开启并继承”。如果角色设定或长期记忆本身过大，需要在设置中精简，创建继承对话不会解决这部分占用。

继承对话会带上：

- 当前角色设定
- 好感度、情绪和关系阶段
- 长期记忆与已确认事件
- 故事目标和章节信息
- 最近对话生成的前情提要

故事模式的继承对话会保留当前关系状态，不会重新从好感度 `0` 开始。上下文百分比是根据文本长度估算的，不等同于模型服务商返回的精确 token 计费值。

## 8. 角色设定与个性化

角色设定面板可以修改：

- 基本信息
- 核心性格
- 语调和表达习惯
- 行为准则
- 身份与背景
- 喜好与厌恶

还可以更换角色头像、用户头像、聊天背景和主题。头像及背景会在保存前进行格式检查和尺寸优化。

## 9. 常见问题

### 为什么按钮是灰色或点了没有反应？

先检查以下状态：

1. 是否已经选择角色并打开聊天存档；角色设置、搜索和导出都依赖当前存档。
2. 是否正在等待模型回复；发送期间部分操作会暂时锁定。
3. 是否打开了过去章节；过去章节是只读的，需要返回当前章节或创建分支。
4. 是否提示角色设定或长期记忆过长；可先在设置中精简。较早对话会按容量自动整理，也可以主动开启继承对话。

### Android 版为什么无法发送消息？

Android 原生版需要自己的 OpenAI 兼容模型接口：

1. 点击“更多”→“模型 API 设置”。
2. 输入服务商提供的 API 地址，通常以 `/v1` 结尾。
3. 输入 API Key，点击“测试连接”。
4. 测试成功后点击“保存”，再返回聊天。

如果测试失败，请检查手机网络、API 地址、Key 是否有效、账户余额，以及服务商是否允许当前模型。API Key 只应填在设置中，不要发送到聊天消息里。

### 为什么角色没有明显体现好感度和情绪？

状态会以语气、称呼、主动性、亲疏距离和情绪反应体现，不会机械地复述数字。好感变化不会越过已确认的关系边界；低情绪通常更简短克制，高情绪通常更轻快积极。

### 为什么我修改了角色设定，旧存档没有变化？

存档会保存自己的角色快照。修改角色后，新建存档会使用新设定；已有存档需要在当前存档中修改并保存。

### 删除存档后还能恢复吗？

不能。删除会移除对应的本地数据库记录和快照，请确认后再操作。

### Web 端和 Android 端的数据会同步吗？

不会自动同步。Web 使用后端 SQLite，Android 使用手机本地 SQLite，两端存档相互独立。

### 换手机或卸载应用会怎样？

Android 聊天数据保存在当前手机的应用数据中。卸载应用可能同时删除本地存档；重要聊天请提前通过“更多”→“导出对话”保存为文本文件。目前没有自动云同步或一键恢复功能。

### 界面仍然显示异常怎么办？

先完全关闭应用再重新打开。如果问题仍然存在，请记录手机型号、Android 版本、出现问题前的操作步骤和错误提示；如果方便，也可以附上不包含 API Key 或私人聊天内容的界面截图。

## 10. 数据与隐私提示

Web 端聊天记录保存在项目后端的 SQLite 数据库中。模型请求会发送到 `.env` 配置的兼容接口。不要在聊天内容或角色设定中填写不希望发送给模型服务商的敏感信息。

## 角色示例与试聊

在角色设置中展开“示例对话与试聊”，填写几段“用户：…／角色：…”示例，示范角色在关心、拒绝或冲突时的表达方式。示例只用于学习风格，不作为本存档已经发生的事情。

输入测试消息后点击“试聊”，使用当前模型接口预览回复。试聊不加入正式聊天，不修改关系、记忆或事项；编辑示例本身仍按角色设置的保存机制保存到当前存档。

每轮正式聊天在原有情感分析和回复请求之外，增加一次场景／事项整理请求；整理可能增加等待时间与模型用量。试聊也会使用模型额度。
