# 角色扮演聊天应用

一个基于 Vue 3 和 Node.js 的智能角色扮演聊天应用，支持自定义角色设定和多模态对话。

## ✨ 特性

- 🎭 **角色扮演**：支持预设角色和自定义角色设定
- 💬 **智能对话**：基于 GLM-4.5V 模型的智能对话
- 🖼️ **多模态支持**：支持图片上传和识别
- 🎨 **精美界面**：使用 Element Plus 构建的现代化 UI
- ❄️ **动态效果**：雪花特效和自定义聊天背景
- 📱 **响应式设计**：支持多种屏幕尺寸

## 🛠️ 技术栈

### 前端
- Vue 3
- Element Plus
- Vite
- Axios

### 后端
- Node.js
- Express
- GLM API

## 📦 安装

### 前置要求
- Node.js >= 20.12.0
- npm >= 10.5.0

### 克隆项目
```bash
git clone https://github.com/naitjc/chat.git
cd chat/qa-app
```

### 安装依赖

#### 后端
```bash
cd backend
npm install
```

#### 前端
```bash
cd frontend-vue
npm install
```

## 🚀 运行

### 配置环境变量

在 `backend` 目录创建 `.env` 文件：
```
GLM_API_KEY=your_api_key_here
PORT=8888
```

### 启动后端
```bash
cd backend
npm start
```

### 启动前端
```bash
cd frontend-vue
npm run dev
```

访问 `http://localhost:5173` 即可使用应用。

## 📁 项目结构

```
qa-app/
├── backend/                 # 后端服务
│   ├── server.js           # Express 服务器
│   ├── .env                # 环境变量配置
│   └── package.json
└── frontend-vue/           # 前端应用
    ├── public/
    │   ├── characters.json # 角色配置
    │   └── avatars/        # 角色头像
    ├── src/
    │   ├── App.vue         # 主应用组件
    │   ├── main.js         # 入口文件
    │   ├── style.css       # 全局样式
    │   └── components/     # Vue 组件
    │       ├── AppHeader.vue
    │       ├── CharacterSettings.vue
    │       ├── ChatArea.vue
    │       ├── PopupModal.vue
    │       └── SnowEffect.vue
    └── package.json
```

## 🎮 使用说明

1. **选择角色**：从下拉菜单选择预设角色或使用自定义模式
2. **配置角色**：设置角色的性格特征、身份背景等
3. **开始对话**：输入消息或上传图片与角色对话
4. **自定义背景**：点击"设置聊天背景"上传自定义背景图

## 📝 角色设定字段

- **角色名称**：角色的名字
- **行为特征**：角色的外在行为表现
- **身份背景**：角色的背景故事
- **性格特征**：角色的性格描述
- **语言风格**：角色的说话方式
- **性别**：角色的性别
- **喜欢的物品**：角色喜好
- **讨厌的物品**：角色厌恶的事物
- **称呼用户的昵称**：角色如何称呼用户

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT
